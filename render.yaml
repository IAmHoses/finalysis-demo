services:
  - type: web
    name: rts-labs-coding-challenge
    env: node
    plan: free
    buildCommand: npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
